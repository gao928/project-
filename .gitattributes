# 批处理/PowerShell 必须保留 CRLF，否则 cmd 解析会出错（中文乱码、语法错乱）
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# shell 脚本用 LF
*.sh text eol=lf

# 二进制资源不要做换行转换
*.png binary
*.jpg binary
*.zip binary
*.whl binary
